// TYPES
import { FC } from "react";

const Navbar: FC = () => {
  return (
    <button className="button__sidebar--open ">
      <div className="bar"></div>
      <div className="bar"></div>
    </button>
  );
};

export default Navbar;
